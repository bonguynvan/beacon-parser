---
"@bonv/beacon-mcp": minor
---

Initial release: an MCP server (stdio, local only) exposing `parse_hit`, `explain_hit`, `diff_hits` and `validate_hits` so AI agents can decode, explain, diff and validate Adobe Analytics hits.
